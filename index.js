// Importing the object dsaData from ds.js
import { dsaData } from "./ds";

// Iterate over the values of dsaData and log each value
Object.values(dsaData).forEach((value) => {
    console.log(value);
});
